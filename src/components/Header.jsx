
const Header = ({title}) => {
    return (
        <div className="bg-sky-700 py-5">
            <h1 className="text-3xl font-semibold text-gray-50  text-center">{title}</h1>
        </div>
    );
};

export default Header;